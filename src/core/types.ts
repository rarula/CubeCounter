export type Question = {
    scene: number[][];
    answer: number;
    choices: number[];
};

export type Coordinates = {
    x: number;
    y: number;
    z: number;
};

export type Size = {
    width: number;
    height: number;
    depth: number;
};
